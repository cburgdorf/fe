Added `StorageBytes::to_memory` to copy stored bytes into memory without ending the contract call, allowing further processing such as hashing or contract deployment. Added `StorageBytes::word_at` for indexed access to individual payload words.
