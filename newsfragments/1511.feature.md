Added `std::evm::encode_msg_calldata` to encode a message selector and arguments into memory for low-level calls. Added `Address::static` for typed `STATICCALL` operations that forward the available gas and propagate the callee's revert data on failure.
