Fixed clean builds depending on Tree-sitter parser generation order by ensuring parser generation precedes the web asset build. Installing the parser's npm dependencies no longer attempts to build an unused Node addon.
